---
title: LogRelay プライバシーポリシー
---

最終更新日：2026年9月22日

本ポリシーは、Androidアプリ「LogRelay」（以下「本アプリ」）が扱う情報と、その保存・送信について説明します。現在一般向けに配布しているのはスマートフォン用の 0.2.0-alpha です。Wear OS 版は一般向けにはまだ配布していませんが、連携時のデータの扱いも以下に記載します。

## 1. 基本方針

本アプリは、記録のためのアカウント登録を求めず、開発者が運用するサーバーへ記録内容を自動送信しません。記録データはまずスマートフォン内のデータベースとアプリ専用ストレージに保存します。ただし、地名の取得、Wear OS 連携、ユーザーが選んだ保存先への書き出し、Android 標準バックアップでは、情報が端末外へ渡る場合があります。詳細は第3・4節をご確認ください。

## 2. 扱う情報

### 記録と位置情報

ホーム画面ウィジェット、アプリ内の追加操作、LogRelay 用 NFC タグを使った記録では、日時を保存します。位置情報への許可があり取得できた場合は、その時点の緯度・経度も保存します。位置情報を許可しない場合や取得できない場合も、日時だけの記録ができます。バックグラウンドで継続的に位置を追跡する機能はありません。

Wear OS 連携を利用する場合、ウォッチで記録操作をした時刻と、ウォッチで位置情報が取得できた場合の緯度・経度を、Google Play 開発者サービスの Wear OS Data Layer を通じてペアリングされたスマートフォンへ送ります。位置情報が取得できなければ、位置なしの記録として扱います。送信待ちのデータは、スマートフォンで処理されるまで Data Layer に保持される場合があります。

地名の表示には Android の Geocoder を使います。この処理では、OS や端末の実装に応じて位置情報が外部の地名解決サービスへ送られる場合があります。

### メモ・分類・写真

ユーザーが入力したメモや分類タグ、記録の日時・位置情報はスマートフォン内のデータベースに保存します。ユーザーが写真を選択または撮影した場合、記録に添付する写真をアプリ専用ストレージに保存します。LogRelay 用 NFC タグには記録内容や位置情報を書き込まず、アプリを起動するための固定のトリガー情報のみを書き込みます。

## 3. 保存とバックアップ

ユーザーは記録を Markdown・CSV などで書き出し、Android の共有機能で他のアプリに渡せます。手動バックアップは、記録・メモ・位置情報・添付写真に加え、ゴミ箱内の記録を ZIP ファイルに保存します。この ZIP はアプリによって暗号化されません。保存先がクラウドストレージの場合、その事業者のサービスにファイルが保存されます。

ユーザーが保存先フォルダと間隔を設定して定期バックアップを有効にした場合、アプリはそのフォルダへバックアップ ZIP を自動作成します。保存先がクラウドと同期されるフォルダであれば、自動バックアップも端末外へ送られる場合があります。アプリは同フォルダ内の LogRelay 自動バックアップを新しいものから5件残し、古いものを削除します。

本アプリは Android 標準バックアップを有効にしています。端末・Google アカウントのバックアップ設定などにより、アプリ内の記録・写真・設定が OS のクラウドバックアップや端末間移行に含まれる場合があります。これは上記の ZIP バックアップとは別の、Android の仕組みです。

## 4. 外部への送信

本アプリには、広告や利用状況分析のための SDK を組み込んでいません。開発者が記録内容を収集するためのサーバーも運用していません。一方、次の場合には、記録または位置に関する情報が開発者以外のサービス・端末へ渡る可能性があります。

- 地名解決に伴う、OS が利用するサービスへの位置情報の送信
- Wear OS 連携時の、ウォッチとスマートフォン間の Data Layer による日時・位置情報の転送
- ユーザーによる共有・書き出し先、または設定したバックアップ先への保存（定期バックアップを含む）
- Android 標準バックアップや端末間移行

これらの保存先やサービスにおける情報の取り扱いは、各サービスの設定・規約にも従います。

## 5. データの削除

アプリ内で記録をゴミ箱に移動し、ゴミ箱から完全に削除できます。ゴミ箱に入れた記録は、アプリ起動時に保存期限（7日）を過ぎていれば自動的に完全削除されます。アプリをアンインストールすると、通常は端末内のアプリデータが削除されます。ただし、手動・定期バックアップの ZIP、Android 標準バックアップ、他のアプリやクラウドに共有したコピーは、アンインストールだけでは削除されません。それぞれの保存先で別途削除してください。

## 6. 権限について

| 権限 | 使用目的 |
|---|---|
| 位置情報（ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION） | 記録時に、許可された場合のみ位置情報を付けるため |
| カメラ（CAMERA） | 記録に添付する写真を撮影するため |
| NFC（NFC） | LogRelay 用 NFC タグの読み取り・書き込みのため |
| インターネット（INTERNET） | OS の地名解決などに必要な通信のため |
| 外部ストレージへの書き込み（Android 9 以前のみ） | 対応する旧端末でのファイル保存のため |

Wear OS 版を利用する場合は、ウォッチ側でも位置情報の許可を求めることがあります。許可しなくても位置なしの記録操作はできます。

## 7. お問い合わせ

本ポリシーや情報の取り扱いについてのご質問は、[sakam72kiratomato+logrelay@gmail.com](mailto:sakam72kiratomato+logrelay@gmail.com) までお寄せください。

## 8. 本ポリシーの変更

機能や取り扱いが変わる場合は、本ページを更新し、最終更新日を明記します。
